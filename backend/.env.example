# InvestIQ Backend Environment Variables
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to git - it's already in .gitignore

# ============================================
# Database Configuration
# ============================================
# Local PostgreSQL (Docker Compose):
DATABASE_URL=postgresql://investiq:<password>@localhost:5432/investiq_db

# For Kubernetes/EKS (when port-forwarding):
# DATABASE_URL=postgresql://investiq:<your-secret-password>@localhost:5432/investiq_db

# For local SQLite (development only):
# DATABASE_URL=sqlite:///./app.db

# ============================================
# Redis Configuration
# ============================================
# Local Redis (Docker Compose):
REDIS_URL=redis://localhost:6379/0

# For Kubernetes/EKS:
# REDIS_URL=redis://redis.investiq.svc.cluster.local:6379/0

# ============================================
# Google Gemini API (for AI features)
# ============================================
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# ============================================
# SMTP Email Configuration
# ============================================
# For Gmail (recommended for development):
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password
EMAIL_FROM=your-email@gmail.com

# For SendGrid (alternative):
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USERNAME=apikey
# SMTP_PASSWORD=your-sendgrid-api-key
# EMAIL_FROM=noreply@yourdomain.com

# Note for Gmail:
# - Enable 2-factor authentication
# - Generate an "App Password" at: https://myaccount.google.com/apppasswords
# - Use the app password (not your regular password) for SMTP_PASSWORD

# ============================================
# Application Configuration
# ============================================
APP_NAME=InvestIQ
APP_ENV=development
LOG_LEVEL=INFO

# ============================================
# Optional: AWS Configuration (for EKS)
# ============================================
# AWS_PROFILE=hackathon
# AWS_REGION=us-east-1

# ============================================
# Optional: CORS Origins (for frontend)
# ============================================
# CORS_ORIGINS=http://localhost:3000,http://localhost:5173
